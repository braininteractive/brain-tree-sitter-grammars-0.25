[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "demo"
requires-python = ">=3.9"
dependencies = [
  "requests>=2.28",
  "pydantic~=2.0",
]

[project.optional-dependencies]
dev = ["pytest", "ruff"]

[tool.ruff.lint]
select = ["E", "F"]
