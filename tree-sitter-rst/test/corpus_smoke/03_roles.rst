Roles
=====

See :ref:`installation` for setup instructions.

The identity is :math:`e^{i\pi} + 1 = 0` in complex analysis.

Call :py:func:`os.path.join` to build file paths.

Use :doc:`usage` and press :kbd:`Ctrl+C` to stop.

Plain interpreted text like `default role` is also allowed.
