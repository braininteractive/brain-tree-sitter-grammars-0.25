{# a single line comment #}
<div>
  {# a comment
     spanning several
     lines #}
  <p>visible</p>
  {# comment with tokens inside: {{ not_output }} {% not_statement %} #}
</div>
{# trailing comment #}
