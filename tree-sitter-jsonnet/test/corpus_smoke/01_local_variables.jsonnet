// Local variables bound at the top of a document.
local greeting = "hello";
local count = 3;
local pi = 3.14159;
local enabled = true;
local nothing = null;

{
  message: greeting,
  total: count * 2,
  circle: pi,
  on: enabled,
  missing: nothing,
}
